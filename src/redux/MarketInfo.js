//action

//thunk

//initialState
const initialState = {};

//reducer
export default function marketInfoReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
